- `TypedDict` provides **type hints for dictionaries with fixed keys**, allowing editors to catch missing keys or incorrect value types before you even run the code. Only used during Dev
```py
def function(variable_name: DataType)  like func(variable: str) 
```