apiVersion: v1
kind: Service
metadata:
  name: internal-lb
  namespace: nginx-app
spec:
  type: LoadBalancer

  selector:
    #app.kubernetes.io/name: api
    app: api
    env: dev
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000

